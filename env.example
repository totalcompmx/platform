# =============================================================================
# TotalComp MX - Environment Configuration Template
# =============================================================================
# INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual secrets
# 3. NEVER commit the .env file to Git (it's gitignored)
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (REQUIRED)
# -----------------------------------------------------------------------------
POSTGRES_USER=compensacion
POSTGRES_PASSWORD=CHANGE_THIS_TO_A_SECURE_PASSWORD
POSTGRES_DB=compensacion

# Generate a secure password with: openssl rand -base64 24

# -----------------------------------------------------------------------------
# Session & Security (REQUIRED)
# -----------------------------------------------------------------------------
COOKIE_SECRET_KEY=CHANGE_THIS_TO_A_32_CHAR_HEX_STRING
SESSION_COOKIE_NAME=session_totalcompmx
COOKIE_SECURE=true

# Generate a secure key with: openssl rand -hex 16

# -----------------------------------------------------------------------------
# External API Tokens (REQUIRED for ETL Worker)
# -----------------------------------------------------------------------------
# Banxico API Token (for USD/MXN exchange rate updates)
# Get your token at: https://www.banxico.org.mx/SieAPIRest/service/v1/token
BANXICO_TOKEN=your_banxico_token_here

# INEGI API Token (for UMA value updates)
# Register at: https://www.inegi.org.mx/app/api/registrar.html
INEGI_TOKEN=your_inegi_token_here

# -----------------------------------------------------------------------------
# Web Server Configuration
# -----------------------------------------------------------------------------
# For LOCAL development (HTTP only):
# BASE_URL=http://localhost:3080
# HTTP_PORT=3080
# COOKIE_SECURE=false

# For PRODUCTION with Auto-HTTPS (Let's Encrypt):
# AUTO_HTTPS_DOMAIN=totalcomp.mx
# AUTO_HTTPS_EMAIL=your-email@example.com
# AUTO_HTTPS_STAGING=false

# -----------------------------------------------------------------------------
# Session & Security
# -----------------------------------------------------------------------------
# COOKIE_SECRET_KEY=generate_a_random_32_char_key
# SESSION_COOKIE_NAME=session_totalcompmx

# -----------------------------------------------------------------------------
# Email Configuration via Resend (Optional - only if you need email)
# -----------------------------------------------------------------------------
# Get your API key at: https://resend.com/api-keys
# IMPORTANT: Use your verified domain (totalcomp.mx) as the From address
RESEND_API_KEY=your_resend_api_key_here
